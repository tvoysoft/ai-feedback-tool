Вот полный комплект документации для GitHub-репозитория.

---

### **README.md (основной файл)**

# 🤖 AI Feedback Tool: Быстрая текстовая правка для чатов с ИИ

**Браузерный userscript для моментального рецензирования и правки ответов языковых моделей (DeepSeek, ChatGPT и других) прямо в интерфейсе чата.**

Зачем часами переписывать промпты, если можно тыкнуть пальцем? Этот инструмент превращает долгий процесс «прочитал → нашёл ошибки → сформулировал запрос на исправление» в три клика: **выделил → выбрал реакцию → отправил**.

<img width="784" height="901" alt="image" src="https://github.com/user-attachments/assets/14ac9ee4-85f4-4d47-b54d-d3aca6c2ef9d" />

<img width="780" height="461" alt="image" src="https://github.com/user-attachments/assets/d9d58846-76af-47b9-b7ed-2e66f8aaacc1" />


## ✨ Возможности

*   **Интерактивное рецензирование**: Выделяйте любой фрагмент ответа ИИ и мгновенно давайте обратную связь.
*   **Богатый набор реакций**: 9 категорий с эмодзи — от простого 👍/👎 до конкретных «Исправить», «Дополнить», «Сократить».
*   **Умное форматирование**: Все ваши пометки автоматически собираются в четкий, структурированный промпт с цитатами, который идеально понимает любая нейросеть.
*   **Работа поверх любого сайта**: Не требует API-ключей, не отправляет ваши данные на сторонние серверы. Работает как userscript в вашем браузере.
*   **Локальное хранение**: Все ваши правки сохраняются локально для каждой сессии чата.

## 🚀 Быстрый старт

### Установка скрипта (через менеджер userscript'ов)

1.  Установите менеджер userscript'ов для вашего браузера:
    *   [Tampermonkey](https://www.tampermonkey.net/) (Chrome, Firefox, Safari, Edge) — **рекомендуется**.
    *   [Violentmonkey](https://violentmonkey.github.io/) (альтернатива).

2.  **[Установите скрипт AI Feedback Tool](https://github.com/tvoysoft/ai-feedback-tool/blob/main/ai-feedback-tool.user.js)** (нажмите на ссылку, менеджер предложит установку).

3.  Откройте [DeepSeek Chat](https://chat.deepseek.com) или другой поддерживаемый сервис. В правом нижнем углу окна ввода появится кнопка **«Собрать правки (0)»** — значит, инструмент готов к работе.

## 🖱️ Как пользоваться

Работа с инструментом сводится к простому циклу:

1.  **Прочитайте ответ** нейросети в чате.
2.  **Выделите мышью** фрагмент текста, который хотите прокомментировать. Рядом с выделением появится меню с реакциями (эмодзи).
3.  **Выберите тип правки** (например, `⚠️ Ошибка` или `➕ Дополнить`). При необходимости добавьте текстовый комментарий во всплывающем поле.
4.  **Повторите** для всех нужных фрагментов ответа. Счётчик на кнопке будет увеличиваться.
5.  **Нажмите кнопку «Собрать правки (N)»**. Весь ваш фидбэк оформится в промпт и автоматически вставится в поле ввода чата.
6.  **Отправьте запрос**. Нейросеть получит точную, структурированную инструкцию и выдаст исправленный вариант.

**Совет:** Для скоростной работы можно использовать **горячие клавиши** `1`-`9` для выбора категорий при открытом меню.

### 📌 Кнопка блокнота (режим работы)

Рядом с кнопкой отправки сообщения находится кнопка в виде **блокнота** (`📝`). Она управляет основным поведением скрипта:
*   **Зелёный блокнот (`📝`)** — **режим активен**. При выделении текста появляется меню с реакциями.
*   **Серый блокнот (`📓`)** — **режим приостановлен**. Выделение текста работает как обычно, меню не появляется. Удобно, если нужно просто скопировать часть ответа.

## ⚙️ Конфигурация и расширение

Инструмент легко адаптировать под другие сайты или изменить визуальный стиль фидбэка, отредактировав конфигурационный блок в начале скрипта.

### Основные настройки (`CONFIG`)

```javascript
const CONFIG = {
  // Селекторы для поиска элементов на странице (могут меняться при обновлениях сайта)
  TEXTAREA_SELECTOR: 'textarea._27c9245', // Поле ввода сообщения
  TEXT_DUPLICATE_SELECTOR: 'div.b13855df', // Элемент-дубликат текста (если есть)
  MESSAGE_CONTAINER_SELECTOR: 'div.ds-message._63c77b1', // Контейнер сообщений в чате
  BUTTONS_CONTAINER_SELECTOR: 'div.ec4f5d61', // Контейнер для кнопок (сюда добавляется наша кнопка)
  SEND_BUTTON_SELECTOR: 'button[data-testid="send-button"], button:has(svg)', // Кнопка отправки

  // Стилистика формируемого промпта
  QUOTE_START: '❝',
  QUOTE_END: '❞',
  DELIMITER_START: '◈◇◇',
  DELIMITER_END: '◇◇◈',
  ITEM_MARKER: '◆',
};
```

### Категории фидбэка (`CATEGORIES`)

Вы можете свободно редактировать, добавлять или удалять категории реакций, изменяя массив `CATEGORIES`:

```javascript
const CATEGORIES = [
  {id: 'like', emoji: '👍', label: 'Нравится'},
  {id: 'dislike', emoji: '👎', label: 'Не нравится'},
  {id: 'error', emoji: '⚠️', label: 'Ошибка'},
  // ... добавьте свою категорию:
  // {id: 'my_category', emoji: '✨', label: 'Моя реакция'},
];
```

**Чтобы добавить поддержку нового сайта**, достаточно обновить CSS-селекторы в объекте `CONFIG`, указав корректные пути к элементам интерфейса целевого чата. Для этого используйте инструменты разработчика в браузере (F12).

## 🧩 Технические детали

*   **Тип**: Браузерный userscript (Tampermonkey/Violentmonkey).
*   **Язык**: Чистый JavaScript (ES6+).
*   **Зависимости**: Отсутствуют.
*   **Хранение данных**: `localStorage` (привязано к ключу сессии `ai_feedback_{pageKey}`).
*   **Принцип работы**: Скрипт инжектится в страницу, находит элементы чата по селекторам, добавляет кастомные обработчики событий и UI-элементы.

## ❓ Частые вопросы (FAQ)

**Q: Безопасен ли этот скрипт? Куда отправляются мои данные?**  
**A:** Абсолютно. Скрипт работает исключительно в вашем браузере. Он не использует внешние серверы, не имеет доступа к вашим API-ключам и не отправляет ваши промпты или историку куда-либо, кроме того чата, в котором вы работаете.

**Q: Скрипт перестал работать после обновления DeepSeek/ChatGPT.**  
**A:** Вероятно, изменилась структура HTML/CSS страницы. Нужно обновить селекторы в объекте `CONFIG`. Создайте Issue в репозитории, и мы поможем с адаптацией.

**Q: Можно ли использовать инструмент в ChatGPT/Claude/других чатах?**  
**A:** В текущей версии он заточен под DeepSeek Chat. Однако, благодаря модульной архитектуре, для добавления поддержки нового провайдера чата достаточно обновить селекторы в конфигурации (раздел «Конфигурация и расширение» выше). Pull requests с конфигами для новых платформ приветствуются!

**Q: Я не программист. Смогу ли я изменить категории реакций?**  
**A:** Да! Это самый простой вид настройки. В начале файла скрипта найдите массив `CATEGORIES`. Вы можете менять эмодзи (`emoji`) и подписи (`label`) у существующих строк или добавлять новые по аналогии.

## Развитие проекта

Этот инструмент родился из личной необходимости и отточен в реальной работе. Если он оказался полезен и вам — здорово!

*   **Нашли баг или есть идея?** Создавайте [Issue](https://github.com/tvoysoft/ai-feedback-tool/issues).
*   **Добавили поддержку нового чата или улучшили код?** Присылайте [Pull Request](https://github.com/tvoysoft/ai-feedback-tool/pulls).
*   **Хотите просто сказать спасибо?** Поставьте звезду (`⭐`) репозиторию — это лучшая мотивация для развития.

---

*Инструмент создан для тех, кто не просто потребляет контент от ИИ, а вдумчиво с ним сотрудничает. Создавайте больше, правьте быстрее.*
