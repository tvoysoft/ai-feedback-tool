# Техническое задание: "Быстрая текстовая правка" (расширение для браузера / userscript)

## 1. Цель проекта
Разработать инструмент для пользователей AI-чатов (Deepseek, ChatGPT, Claude, Gemini и аналоги), позволяющий быстро маркировать проблемные участки в тексте (как в запросах, так и в ответах) и автоматически формировать из этих пометок структурированный промпт для исправления.

## 2. Основной пользовательский сценарий
1. **Пользователь читает** текст в интерфейсе AI-чата.
2. **Выделяет** проблемный фрагмент текста (слово, предложение, абзац).
3. **Выбирает** категорию правки из предустановленного списка и при необходимости добавляет текстовый комментарий.
4. **Визуально видит** пометку на странице (рамка + иконка категории).
5. **По окончании** нажимает кнопку "Собрать правки (N)", которая:
   - Очищает поле ввода чата
   - Вставляет структурированный промпт со списком правок
   - Устанавливает курсор в конец вставленного текста
   - Очищает все визуальные пометки со страницы

## 3. Функциональные требования

### 3.1. Ядро функциональности
1. **Работа с выделенным текстом**:
   - При выделении любого текста на странице AI-чата появляется плавающая кнопка вызова меню правок
   - Меню должно быть контекстно-зависимым (показываться рядом с выделенным текстом)

2. **Меню правок** содержит:
   - Список предустановленных категорий (6-8 вариантов с иконками)
   - Текстовое поле для пользовательского комментария (опционально)
   - Кнопки "Сохранить" и "Отмена"

3. **Предустановленные категории** (с иконками):
   - `🔄 Перефразировать` - изменить формулировку
   - `❌ Удалить` - убрать фрагмент
   - `🔎 Раскрыть` - добавить детали
   - `➕ Дополнить` - расширить мысль
   - `⚠️ Ошибка` - исправить фактическую ошибку
   - `📏 Сократить` - сделать короче
   - `🎯 Уточнить` - конкретизировать
   - `⚖️ Уравновесить` - добавить альтернативный взгляд

4. **Визуальная обратная связь**:
   - Помеченный фрагмент текста обводится цветной рамкой
   - Справа от фрагмента добавляется иконка выбранной категории
   - При наведении на иконку показывается всплывающая подсказка с полным комментарием

### 3.2. Управление правками
1. **Сборка промпта**:
   - Кнопка "Собрать правки (N)" отображается над/рядом с полем ввода чата
   - Число N отображает количество необработанных правок
   - При клике формируется структурированный промпт по шаблону (см. раздел 4)

2. **Сохранение состояния**:
   - Все правки сохраняются между обновлениями страницы
   - При повторном открытии той же страницы чата все пометки восстанавливаются

3. **Очистка**:
   - После формирования промпта все визуальные пометки удаляются
   - Данные о правках очищаются из хранилища

## 4. Формат выходного промпта

### 4.1. Шаблон промпта
```
Пересмотри текст и внеси следующие правки:

1. В тексте: "[цитата фрагмента 1]"
   Категория: [название категории 1]
   Комментарий: [пользовательский комментарий 1, если есть]

2. В тексте: "[цитата фрагмента 2]"
   Категория: [название категории 2]
   Комментарий: [пользовательский комментарий 2, если есть]

...

Всё остальное оставь без изменений.
```

### 4.2. Требования к генерации:
- Каждая правка нумеруется
- Цитата фрагмента берётся точно, как она была выделена пользователем
- Если пользовательский комментарий отсутствует, строка "Комментарий:" не включается
- Промпт вставляется в поле ввода, а не отправляется автоматически

## 5. Технические требования

### 5.1. Архитектура
**Вариант A: Userscript (рекомендуемый)**
- Использовать менеджер userscript'ов (Tampermonkey/Violentmonkey)
- API для хранения: `GM.getValue` / `GM.setValue`
- Кросс-браузерная совместимость через стандартные userscript API

**Вариант B: Расширение для браузера**
- Manifest V3 для Chrome
- Отдельные файлы: manifest.json, content.js, background.js, popup.html, styles.css

### 5.2. Структура данных
```javascript
// Объект правки
{
  id: 'уникальный_идентификатор',
  pageUrl: 'URL страницы, где сделана правка',
  targetText: 'выделенный текст',
  category: 'код_категории',
  customComment: 'пользовательский комментарий или null',
  timestamp: 'время создания',
  context: 'небольшой фрагмент текста вокруг выделения для контекста'
}

// Хранилище
{
  'chat.openai.com/c/...': [
    { /* правка 1 */ },
    { /* правка 2 */ }
  ]
}
```

### 5.3. Производительность
- Минимальное воздействие на производительность страницы
- Отложенная загрузка элементов интерфейса
- Оптимизация обработки событий (дебаунсинг)

## 6. Интерфейс и UX

### 6.1. Визуальный дизайн
- **Цветовая схема**: жёлтый фон (#fff9c4) + оранжевая рамка (#ffb74d)
- **Иконки**: эмодзи или SVG-иконки для категорий
- **Шрифты**: наследование шрифтов основной страницы
- **Анимации**: плавное появление/исчезновение элементов (200-300ms)

### 6.2. Расположение элементов
1. **Кнопка вызова меню**: появляется в конце выделенного текста
2. **Меню правок**: позиционируется рядом с выделением, не перекрывая текст
3. **Кнопка "Собрать правки"**: фиксированная позиция в правом нижнем углу поля ввода чата
4. **Иконки правок**: inline, сразу после помеченного текста

## 7. Совместимость

### 7.1. Поддерживаемые платформы
- Deepseek (Deepseek)
- ChatGPT (OpenAI)
- Claude (Anthropic) 
- Gemini (Google)
- Perplexity
- Любые другие чат-интерфейсы с аналогичной структурой DOM

### 7.2. Браузеры
- Google Chrome 90+
- Mozilla Firefox 90+
- Microsoft Edge 90+
- Safari 15+ (для варианта с userscript)

## 8. Безопасность и конфиденциальность
- Все данные хранятся локально
- Никакая информация не отправляется на внешние серверы
- Очистка данных при закрытии вкладки (опционально)
- Доступ только к тем доменам, которые явно указаны в манифесте
